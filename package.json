{
  "name": "goodreads_shelf_compare",
  "version": "1.0.0",
  "description": "Compares the books on a GoodReads shelf belong to two users and returns shared books.",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node out/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dayna Lee Talbot",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.11",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.118",
    "@types/request": "^2.48.1"
  }
}
